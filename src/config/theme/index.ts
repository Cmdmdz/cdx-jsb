'use client'
import { inputTheme } from '@/config/theme/input.theme'
import { dialogTheme } from '@/config/theme/dialog.theme'
import { textareaTheme } from '@/config/theme/textarea.theme'
import { selectTheme } from '@/config/theme/select.theme'

export const themeConfig = {
    dialog: dialogTheme,
    input: inputTheme,
    textarea: textareaTheme,
    select: selectTheme
}
